# AGENTS.md — конституция агента сайта Interfood/nilov catering

> **QUICK-INDEX:** §0–3 — идентичность/заповеди/грабли/инфраструктура — читать
> ВСЕГДА. §4–7 — конвейер фото/состояние/тон/правила файла. §8 — сжатые уроки
> циклов c56–c81 (1–2 строки на цикл; полные истории — `docs/AGENTS-HISTORY.md`
> и `git log worklog.md`). Worklog — последние 2 секции только.

> **Читать §0–8 перед ЛЮБОЙ работой.** Это дистиллят 81 цикла, включая провалы,
> пойманные пользователем. Моё качество = качество моей проверки: что проверка
> не спрашивает — то она не видит.

---

## 0. Кто я и что здесь строю

Сайт премиального кейтеринга «Interfood / nilov catering» (СПб, 16 лет, 2400+
событий). Репозиторий — рабочий цех: копируем лучшие решения с эталонных сайтов
(Awwwards, мировые кейтеринги), доводим до уровня выше оригинала.

## 1. Десять заповедей (нарушал — платил циклами исправлений)

1. **Спрашивай о запрещённом, не только о желаемом.** Проверка «есть ли
   стейк?» пропустила инфографику с колесом температур; «MATCH=yes»
   благословил alamy-вотермарки. Любая проверка фото/кода обязана явно
   перечислять запреты: текст, вотермарки, логотипы, схемы, коллажи.
2. **Глазам VLM не верить — верить измерениям.** VLM «видел» обрезанные
   корешки (замер: в пределах), «3 строки» заголовка (реально 1). Скриншот —
   гипотеза, rect — факт. В c81 VLM галлюцинировал ~60% CRITICAL-клеймов
   (критик E) — в таблицу дефектов только измеренное.
3. **Пользователь — лучший критик.** Каждая придирка циклов 51–54 — реальный
   дефект, который пропустили VLM и субагенты. Не защищать работу — чинить.
4. **Каждый элемент интерфейса отвечает «что он делает для читателя
   СЕЙЧАС?»** Не отвечает — удалить.
5. **Заголовки никогда не рвутся посреди слова.** `overflow-wrap: normal`,
   перенос по пробелам, ширину ограничивает сетка.
6. **Декорация дешевле истины.** Вкусовые правки юзера отменяют любые
   «улучшения» агента.
7. **Копирайт — сенсорность + конкретика, ноль канцелярита.** Запрещены:
   «техтайминг», «базовый», «и настроение», списки существительных.
8. **Фото — главная валюта кейтеринга.** Замена = полный конвейер §4.
   «Подходит по смыслу» ≠ «подходит по качеству»: одна лига кинематографичности.
9. **Верификация живого — только живым.** agent-browser не применяет viewport
   после relaunch без проверки `window.innerWidth`; fine-pointer — Playwright.
10. **Не «лучшее, на что способна» — а «лучшее, что проверяемо».** Оценка
    VLM 7–9 одному кадру = шум. Стандарт готовности: слепые критики не
    находят реального + измерения зелёные + юзер доволен.

## 2. Технологические грабли (все — живые шрамы)

| Грабля | Симптом | Фикс |
|---|---|---|
| next/font vars на `<body>` | `:root`-токены invalid → ui-sans-serif | vars на `<html>` |
| sharp AVIF-энкодер висит | `/_next/image` с avif не отвечает | `formats:["image/webp"]` (только webp) |
| Замена файла по тому же URL | браузер отдаёт старое | новое имя файла (cache-bust) |
| `body { overflow-x: hidden }` | ломает sticky | `overflow-x: clip` |
| framer `clipPath` string | рендерит `none` | SVG clipPath / ref.style |
| `static` под absolute-слоем | контент тонет | `relative` + z-index |
| React Compiler deps | memo error | все читаемые значения в deps |
| MultiEdit «атомарность» | часть edits применяется | rg-проверка фактического состояния |
| scrollIntoView во время анимации | цель устаревает | таймер после завершения (600ms) |
| image-search/VLM rate-limit | 429 | последовательно, пауза 12–30с |
| Lenis + scrollIntoView | конфликт smooth | проверять позицию |
| **c81:** WAAPI-press поверх CSS `transform: translate(-50%,-50%)` | кнопка «телепортируется», тап мимо | кейфреймы через свойство `scale` (не `transform`) + `transform-origin: 0 0`; `composite:'add'` НЕ надёжен при одновременном CSS-transition на transform (баг Chromium) |
| **c81:** `naturalWidth` в Playwright-эмуляции | показывает 390 при теле ответа 828×553 | мерить сеть/тело ответа, не naturalWidth |
| **c81:** Lighthouse mobile дисперсия | TBT 1140–7570ms между прогонами, score ±8 | гейт по ≥3 прогонам + реальной пробе (Playwright + CPU throttle) |
| **c81:** строки с кавычками через Bash-транспорт | «порча» селекторов | проверка только Read-инструментом (§51) |

## 3. Инфраструктура

```bash
bun install && prisma generate + db push   # DATABASE_URL=file:/home/z/my-project/newsite/db/custom.db (абсолютный!)
pm2 start ecosystem.config.js              # interfood-catering-dev, порт 3001
bun run lint && bun run typecheck          # оба зелёные перед коммитом — обязательно
# прод для Lighthouse: bun run build → cp -r .next/static .next/standalone/newsite/.next/
#   + cp -r public/. .next/standalone/newsite/public/ → pm2 start .next/standalone/newsite/server.js
#   --name interfood-prod-lh с env PORT=3002 HOSTNAME=127.0.0.1 (пример c81)
```

- **Порты:** 3000 — родительский sandbox (НЕ ТРОГАТЬ); 3001 — dev этот сайт;
  3002 — временный prod (для Lighthouse/e2e).
- **Lighthouse:** CHROME_PATH=/home/z/.cache/ms-playwright/chromium-1234/
  chrome-linux64/chrome + `--chrome-flags="--no-sandbox --headless=new
  --disable-gpu --disable-dev-shm-usage"`; вкладка крашится от OOM в конце —
  JSON всё равно полный.
- **Коммиты:** Conventional Commits; push БЕЗ force; перед push —
  `git diff --cached` глазами.
- **Субагенты:** каждый читает `/home/z/my-project/worklog.md` до работы и
  дописывает секцию после. Длинные задачи дробить (context-deadline!).
- **Скиллы:** web-search (версии/паттерны), image-search (фото), VLM
  (`z-ai vision`), agent-browser (e2e), Playwright (fine-pointer/точные
  замеры). Исследуй веб ПЕРЕД решением — «не изобретать колесо».

## 4. Конвейер замены фото (кровь циклов 51–55)

1. `z-ai image-search` — 8 кандидатов, последовательно с паузами.
2. Отсев: ≥1200px шириной, горизонтальные > портретных (панель ~2.1:1).
3. Строгий скан каждого: «есть ли текст/вотермарки/логотипы/инфографика?»
4. Гейт: «CINEMATIC=yes/no» против эталонного листа юзера.
5. Проверка кропа под аспект панели (sharp).
6. Конвертация: webp q82, width 1920; НОВОЕ имя файла.
7. Скриншот ПАНЕЛИ + VLM-чек в контексте.
8. **c81-дополнение:** rendered×2 ≤ natural запрошенного варианта — мерить
   через тело ответа (не naturalWidth, см. §2).

## 5. Текущее состояние витрины (c81)

- Секции: tott-hero (CSS-анимации, видео 720/480 по платформе), services-
  аккордеон, menu-каталог, events-карусель, founder, FAQ, hacc-booking
  (калькулятор+форма, SSR-shell с якорями), footer (espresso).
- Анимации: hero/preloader — чистый CSS (LCP-безопасно); ниже фолда —
  framer-motion/gsap; press-тактилы — WAAPI scale-свойство.
- API: 3 POST с token-bucket rate-limit, zod, серверная нормализация
  телефона; заголовки: CSP(+Metrika/frame-ancestors/UIR), HSTS, COOP.
- Прод-метрики c81: desktop Perf 0.95, mobile 0.60, A11y 0.97, BP/SEO 1.0.

## 6. Тон бренда

Тёплый, чувственный, точный. «Еда как искусство»: сезонность, руки поваров,
ритуал застолья. Хук ≤ 90 знаков, глагол + сенсорная деталь. Запрещены клише
(«вкусно и недорого», «индивидуальный подход») и закупочный жаргон. Проверка
хука: «можно сказать шефу вслух за его столом?»

## 7. Что добавлять в этот файл

Только новые заповеди/грабли с доказательством (цикл, коммит, симптом).
Длинные истории — в `docs/AGENTS-HISTORY.md`. Файл < 300 строк: читается
КАЖДЫЙ раз, ценность в плотности. Секция §8 — сжатый индекс уроков.

## 8. Сжатые уроки циклов (детали: docs/AGENTS-HISTORY.md, git log worklog.md)

- **c56 §28:** VLM-чек обязан спрашивать о запрещённом (объектный гейт).
- **c57 §29:** стандарт 10/10 = слепые критики ничего реального не находят;
  rate-limit image-search лечится паузами.
- **c58 §30:** данные меню — один источник; мёртвый CSS вычищать.
- **c59 §31:** слепой конфлав критиков ловит то, что пропускает владелец;
  цены — в один экран.
- **c60 §32:** scroll-cue и «правда фото» — дефекты, найденные юзером.
- **c61 §33:** reveal-all контент, PDF-редизайн, фото одной лиги.
- **c62 §34:** fixed-attachment параллакс мёртв на iOS — transform-only.
- **c63 §35:** scroll-driven лента: MotionValue иерархия, кириллица-фоллбек.
- **c64 §36:** сцена «смета-чек» — WAAPI-замеры, live-тексты от часов.
- **c65 §37:** слайдер-перф, IO-полоса входа, React Compiler — 15 правок.
- **c66 §38:** AVIF-декод в Chromium мылит (×0.45) — webp-only; клип режет
  тень; ретаргет-скролл после завершения анимации.
- **c69 §39:** ребрендинг субагентами: параллельное владение файлами, 6 волн
  критиков, SHIP-гейт.
- **c70 §40:** fresh-clone деградация (bun install/prisma/env абсолютные);
  API-гигиена.
- **c71 §41:** 4 волны слепых критиков; LLM-SEO (llms.txt); wow-мобайл;
  перф-бюджеты.
- **c72 §42:** hero = картина, конверсия ниже фолда (указание владельца).
  §43: перф-гейты ссылаются на числа — числа меняются, гейты пересматривать.
- **c73 §44:** позиция курсора НИКОГДА не анимируется; Turbopack-CSS-грабли
  после рестарта.
- **c74 §45:** нативные scroll-driven (view()) — верификация двухпозиционная.
- **c75 §46:** cookie-баннер — живой замер высоты, не хардкоды.
- **c76 §47:** flex-shrink против bleed-слоёв; inert-панели дают ЛОЖНЫЕ
  a11y-флаги — проверять фактическую доступность.
- **c77 §48:** логотип — источник правды = активы владельца.
- **c78 §49:** скрытые микровзаимодействия — верификация мелких частиц.
- **c79 §50:** touch-анимации: CLI-латентность ломает тайминги —
  самотаймированный eval; --full-скриншот на тяжёлой странице → CDP-вис.
- **c80 §51:** Bash-транспорт «съедает» кавычки — строки проверять Read-ом.
- **c81 §52:** Lighthouse+3 волны слепых критиков. LCP-текст НЕ гейтится JS
  (CSS-анимации, SSR-видимость); прелоадер — CSS-таймлайн; WAAPI-press —
  scale-свойство + transform-origin 0 0; naturalWidth в эмуляции врёт; LH
  mobile дисперсия ±8 — гейтить ≥3 прогонами; субагенты падают по
  context-deadline — дробить на completion-волны; cookie-карточка
  координируется через --cookie-banner-h (FAB/sticky-bar лифты). ОТКРЫТО:
  lazy-hydration секций (mobile TBT~1.5s — единственный путь к 70+);
  MUX-токены в git-истории ВАЛИДНЫ — ротация у владельца; nonce-CSP;
  полный SSR формы; hires-исходники gamma/furshet; шрифтовая диета;
  hero-CTA в первый экран (решение владельца).

---

*Файл сжат c81 (994→~190 строк) по §7. Полные истории циклов:
`docs/AGENTS-HISTORY.md`, `git log worklog.md`.*

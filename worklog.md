# Worklog — Catering Project (сжатый c84; полная история в git log и docs/AGENTS-HISTORY.md)

## Индекс прошлых задач (детали: git log worklog.md, тег каждого Task ID)
2-research · 2-A/2-B · 3-A/3-B/3-C/3-implement · 4-C/4-D/4-E · 5 · 6-A…6-F · 7-A/7-B · cycle-26 · cycle-28 · 37 · 43 · 43-R4 · 44 · C44-A/C44-C · 45 · cycle-46…48 · 58…63 · c64-c79 (см. AGENTS.md §36-§50) · c80 · c81 · c82 · c83 · c84-research

Ключевые вехи: 6-x — ресёрч/извлечение контента с 22 эталонных сайтов; cycle-26+ —
построение витрины; c55 — аккордеон услуг + фото-конвейер; c62-c64 — сцена booking;
c69 — ребрендинг на субагентах; c71 — 4 слепых критика + LLM-SEO; c72 — hero=
картина; c74-c79 — нативные scroll-эффекты, логотип, скрытые микровзаимодействия,
touch-анимации; c80 — полный QA fresh-clone (0 багов); c81 — Lighthouse+3 волны
слепых критиков + фиксы; c82 — качество моб-hero-видео (портрет-кроп); c83 —
анимации в каждый блок + прод-гейт сборки; c84 — мобильный UX/lite-режим/
калькулятор-пакеты/per-tariff PDF (12 задач владельца).

---

Task ID: c84-research
Agent: web-research subagent (Z.ai Code)
Task: веб-ресёрч мобильного UX — 7 проблем (scroll-hints, морфинг слов,
калькулятор, Data Saver, слабые девайсы, мобильная типографика, PDF
per-tariff). Паттерны 2024-2026, только проверенные живые источники.

Stage Summary:
- ~50 web_search + ~35 page_reader; полный отчёт с код-рецептами:
  docs/C84-MOBILE-UX-RESEARCH.md. Коммит 0eeb569.
- Топ-рецепты: peek-above-fold + pulsing scroll-cue (NN/G illusion-of-
  completeness 6/8 юзеров); CSS scroll-progress + счётчик секций;
  WordRotate/RotatingText (SSR-статика → морфинг после гидратации);
  device-gate lite (saveData||2g||deviceMemory≤2||cores≤4, Osmani);
  калькулятор = одна страница + sticky-сводка + автоскролл (NN/G);
  PDF = dynamic import по клику / route handler.

---
Task ID: c84
Agent: main (Z.ai Code; оркестратор) + субагенты прошлой сессии
(Impl-A/B/C/D, F1-фикстеры, критики M1/M2/P1) + слепые критики A/B/C
(волна оркестратора)

Task: 12 задач владельца — мобильная дружелюбность сайта: Android
эконом-режим, слабые девайсы (тормоза), hero-заголовок/кнопка, морфинг
«искусство», мелкие шрифты, scroll-подсказки, калькулятор (пакеты +
допуслуги + подсказки), понятность тарифов, PDF per-tariff.

Work Log (оркестратор, продолжение прерванной сессии):
- Наследие прошлой сессии: ~2000 строк незакоммиченных изменений
  (lite-режим, hero-peek 92svh + bottom-fade + cue-exit, морфинг слов,
  калькулятор пакеты/допы/sticky/автоскролл, чипы тарифов в дровере,
  per-tariff PDF, типографика). Линт+тайпчек зелёные, верифицировано
  живьём (hero/CUE/peek, морфинг 2.6с, калькулятор 134 100→149 100→
  157 100 ₽, лид № 1009-0909 с пакетом и допами в метe, PDF 141KB
  валиден, кириллица чистая).
- НАЙДЕН ДЕФЕКТ (MAJOR, замер 390×844): клик по чипу тарифа в дровере
  вёл НИЖЕ табов — tabs top −311px, юзер не видел применённый пресет.
  Root cause: цель скролла — высокий .hmenu__item c block:"center"
  (нативная lite-ветка). Фикс: цель .hmenu__details (label+табы+PDF),
  block:"start", scroll-margin-top 96px.
- ВТОРОЙ ДЕФЕКТ (интермиттящий, fresh-load): Lenis-ветка глотала скролл
  МОЛЧА. Инструментация (патч scrollTo + лог всех wST/sIV) поймала:
  lenis.scrollTo вызван, кадров НЕТ. Root cause: после дровер-лока
  (body position:fixed → документ схлопнут) внутренний limit Lenis
  устаревает до 0 → clamp(0, 5584, 0) = 0 === targetScroll →
  early-return. Это та же грабля, что у unlock-restore site-header
  (lenis.resize() перед scrollTo). Фикс: lenis.resize?.() + offset 0
  (Lenis сам читает scroll-margin-top цели — ручной −96 считал дважды,
  посадка 148 вместо 96).
- Двухтактная коррекция посадки (паттерн «пере-якорь»): такт 2 через
  720мс — дельта >24px → доводка, с гвардом «юзер уехал сам» (±140px).
  Замеры после фикса: lite 79-102px / Lenis 70-75px — табы видимы,
  3/3 свежих загрузок в ОБЕИХ ветках.
- ВОЛНА СЛЕПЫХ КРИТИКОВ (3 параллельных, каждый не знал о других):
  - Критик C (перф+код+a11y): REJECT — 2 MAJOR: (1) lite-карта теряла
    role/tabIndex/onKeyDown при живом onClick (WCAG 2.1.1 Level A);
    (2) контраст «Выбрать тариф меню» 4.08:1 (13px/700 ≠ large text).
    +MINOR: такт-2 без гварда; lite-кэш мог перевернуться true→false;
    sticky-бар посадка 64px; NIT: 34px PDF-пилюля на iPad-landscape,
    font-700 шум, морфинг без lite-гейта. Инварианты: lite полный
    (0 mp4/0 jspdf на старте, 60.5 fps против 20-35 full на том же
    железе), гидро-паритет, mount-race закрыты, PDF вне бандла.
  - Критик A (моб-UX): APPROVE — 2 MINOR: 41 текст <12px (min 11px в
    services/menu/booking); табы тарифов ниже первого экрана #menu.
  - Критик B (функционал e2e): APPROVE — вся математика зелёная
    (пересчёты, аддоны, слайдер, sticky-синхронизация, лид № 1009-0955
    «Банкет · Премиум · 100 гостей · ~709 000 ₽», валидация, PDF
    Премиум 5350₽ сверeн с pricing.ts, API 400-гигиена, БД почищена).
    Один UNCONFIRMED-скачок состояния (пакет+меню при клике по аддону)
    не воспроизводится и не объясним кодом — вероятен дрейф координат
    agent-browser при Lenis-скролле (§51-подобная слепота инструмента).
- ФИКСЫ волны (F3): lite-карта — role=button/tabIndex/onKeyDown с
  window.open (клавиатура+SR); контраст метки /60→/70 (~5.3:1);
  гвард такта-2; односторонний lite-кэш (lerp только вверх, фикс
  рассинхрона хук/модуль); sticky-bar scrollToCheck offset 0;
  PDF-пилюля 44px на таче 1024-1279; font-700 шум убран; морфинг
  слов lite-гейт. Типографика (задача 5): 11px→12px, 0.68/0.72rem→
  0.75rem в живых caps-метках services/menu/booking (rack-label,
  tag, «за гостя», hint, тики-пресеты, сноска чека, hb-safe) —
  min кегль страницы замерен 12px (был 11px), tracking не тронут.
- Финал: lint 0, typecheck 0, консоль 0 ошибок, overflowX 0 на всех
  остановках, десктоп-регрессия чистая (hero 828px/peek 72px,
  scroll-progress scaleX 0.30 @5000px).

Stage Summary:
- 12 задач владельца закрыты: (1) Android эконом-режим → lite-гейт
  (saveData/2g/mem≤2/cores≤4): 0 mp4 на старте, Lenis/gsap/canvas/
  растры футера выключены, карта=внешняя ссылка, функциональность
  та же; (2) слабые девайсы → тот же lite + CSS-гейты (60.5fps);
  (3) hero-заголовок/кнопка → перекрытий нет (замер+VLM); (4)
  морфинг «искусство→ритуал→спектакль→кино» 2.6с, SSR-статика,
  IO-пауза, RM/lite-гейты; (5) шрифты → min 12px; (6) scroll-подсказки
  → cue «Листайте» + fade-exit + hero-peek 92svh + scroll-progress;
  (7) пакеты в калькуляторе → радиогруппа Базовый/Стандарт/Премиум;
  (8) допуслуги → 6 чекбоксов, живой пересчёт, в лид; (9) подсказка
  после формата → автоскролл + «Пакет и гости — ниже ↓»; (10) чипы
  тарифов в дровере → пресет+скролл к табам; (11) PDF per-tariff →
  dynamic import, валидная кириллица; (12) общий моб-UX → волны
  критиков APPROVE.
- ГРАБЛИ цикла (→ AGENTS.md §2): Lenis stale-limit после body-lock —
  scrollTo МОЛЧА клампится к 0 и early-return'ит (call есть, кадров
  нет) — всегда lenis.resize() перед scrollTo после дровер-лока;
  Lenis сам читает scroll-margin-top цели — ручной offset поверх
  CSS-маржина считается дважды; fresh-load layout-оседание ~44px —
  программные посадки корректировать вторым тактом с гвардом.
- ОТКРЫТО (c85): табы тарифов ниже первого экрана #menu (критик A
  MINOR — Layouts-решение владельца); дефолт калькулятора «Банкет»
  (критик B INFO — подтвердить намеренность); lazy-hydration секций
  (перенос c81/c83); MUX-ротация у владельца; nonce-CSP.
- Коммит c84: код+доки, push без force.

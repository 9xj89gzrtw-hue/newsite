# Worklog — Catering Project (сжатый c89; полная история: git log worklog.md, docs/AGENTS-HISTORY.md)

## Индекс прошлых задач (детали: git log, тег Task ID)
2-research · 2-A/2-B · 3-A/3-B/3-C · 4-C/4-D/4-E · 5 · 6-A…6-F · 7-A/7-B · cycle-26…48 · 58…63 · c64-c87 (AGENTS.md §36-§56) · c88 · c89

Ключевые вехи: 6-x — ресёрч с 22 эталонов; cycle-26+ — витрина; c55 — фото-конвейер;
c69 — ребрендинг субагентами; c71 — LLM-SEO; c80 — QA fresh-clone; c81 — Lighthouse+критики;
c83 — анимации везде; c84 — мобильный UX; c85 — перф/iPhone; c86 — правки владельца+критики;
c87 — 44 реальных фото владельца; c88 — фото в ленту, видео-карусель возвращена;
**c89 — большой пакет правок владельца: 6 форматов услуг, новые цены/минимальные
заказы, видео с Диска, контакты 2 колонки, реквизиты ИП Ниловой, 4 волны критиков**.

---
Task ID: c88 (сжато)
Agent: orchestrator (критики волн 1-2)
Task: видео-карусель возвращена на рендер; 44 фото — в ленту GammaMarquee
за «Кейтеринг как искусство».

Stage Summary:
- Лента: тайлы единой высоты, пропорция кадра инлайн; цикл 170с ≈ 110-115 px/s.
- Фиксы волн: z модалки 95 (кука/FAB больше не поверх), шов −50% точен.
- Гейты: 2 волны → APPROVE; lint/tsc/консоль чистые. Коммиты c88.

---
Task ID: c89
Agent: orchestrator (7 субагентов-реализаторов + 4 волны слепых критиков)
Task: большой пакет правок владельца (ауг 2026): карусель/услуги/меню/цены/
видео/калькулятор/контакты/подвал/UX. Коммит aa62892.

Work Log (сжато; полная спека — сообщение коммита aa62892):
- Карусель: −8 фото (07/10/11/22/25/28/41/44), 36 осталось, 170с→137с.
- Глава 01: один аккордеон, 6 форматов (Свадьбы/Корпоратив/Шоу-станции/
  Выездной бар/Вегетарианское и халяль/Гастро-боксы), CTA→#calculator.
- Глава 02: фикс прыжка банкетных табов (scrollTop-сохранение + анимация
  высоты рэка по паттерну is-animating/expandList); BBQ 2900/4450
  +листовой салат 150г+ассорти летних овощей 150г; веган 3200/4250; «от»
  везде (панель/табы/PDF); hookNote доставки: мин 17 400 ₽/48ч; сезонный
  коэффициент ×1,15 удалён отовсюду (pricing no-op, чек, PDF, llms).
- Видео (Яндекс.Диск, все 1080×1920): GG-showcase = 16:9 центр-кроп
  1280×720 + портрет 720×1280 для тача (паттерн tott-hero, гард по src);
  карусель = 6 вертикальных клипов c89-event-1..6 (720×1280, ≤8.4МБ),
  модалка 9:16 (height min(86vh,140vw)), постеры, VLM-честные подписи.
- Калькулятор: snack-box flat calcPerGuest=1200 (пакеты НЕ выбираются);
  MIN_ORDER (85к банкетные/50к кофе/17.4к доставка) — кламп итога +
  честная строка «Минимальный заказ формата +N ₽»; ADDONS waiters→
  furniture «Аренда мебели» 15000; сезонная строка/предупреждения удалены.
- Контакты: 2 колонки (Связь: Телефон/ТГ/Ватсап/Макс +7 911 826-39-26/
  Смс/Адрес | Соцсети: IG/VK/ТГ-канал @nilov_official/YouTube/Rutube);
  карта → подвал (lazy iframe); #contact → ContactsZone (форма = #lead-form,
  открывается только CTA); мета/JSON-LD/llms/yandex.txt синхронизированы
  (ИП Нилова Анастасия Дмитриевна, ТБанк, dmitry_nilov@mail.ru, 2007).
- UX: BackToTop возвращён (мобайл+десктоп); HoverScramble из хедера убран;
  КРИТИЧЕСКИЙ ФИКС drawer-навигации: location.hash-прыжок откатывался
  Lenis на таче → closeAndNavigate всегда через __lenis.scrollTo +
  тихий pushState (hashchange-слушатели — только косметика шапки);
  капс-лейблы 12→13px (--hb-caps-label-fs); founder-scene-v5 (без разводов).
- Верификация: Playwright-скрипты золотого пути (все числа чека сходятся);
  4 волны критиков: W1 код REJECT→фиксы (mailto лида из конфига!, llms,
  660/1200 split-brain), W1 визуал+аудитор 23/23 APPROVE, W2 код APPROVE
  (yandex.txt), W2 UX — drawer-баг найден и закрыт, W3 гейт (JSON-LD
  founder→«Дмитрий Нилов»), W4 финал APPROVE. lint/tsc/консоль чистые.

Stage Summary:
- ВСЁ из письма владельца выполнено и проверено (23/23 требований PASS).
- Ссылки-заглушки (ждём от владельца): Rutube — поиск по бренду (канал
  не найден), Макс — max.ru (диплинка по номеру нет; профиль 404).
- Меню-каталог доставки закусок показывает à la carte от 660 ₽ (пакеты),
  калькулятор — единые 1200 ₽/чел: осознанное разделение (задокументировано).
- pm2 :3001 (interfood-catering-dev) стабилен; порт 3000 — родительский
  sandbox, не трогать. ffmpeg на этой машине: только background+polling
  (foreground убивается SIGKILL по завершении — см. урок c89 в AGENTS.md).

---
Task ID: c90
Agent: orchestrator
Task: фикс упавшего деплоя nilovcatering.ru (юзер: «не получилось задеплоить»).

Work Log:
- Диагноз по GitHub API: runs aa62892/8d3eb34 (16.09) — build/export OK,
  падает шаг rsync: «Connection closed by host port 22», rc=255 (SSH-уровень).
  deploy.yml/secrets между успехом 12.09 и провалом не менялись → transient
  на стороне хостинга (блок IP раннера GitHub у SpaceWeb).
- Ручной workflow_dispatch на main → успех; живой сайт проверен: c89-маркеры
  на месте (826-39-26, 17 400, ИП Нилова), старые удалены («Как мы работаем»,
  «Сезонный коэффициент»); 10/10 sampled media = 200.
- deploy.yml: rsync-шаг получил 3 попытки с паузой 45с + ConnectTimeout=20
  + ::group::-логика (устойчивость к transient-блокам IP раннеров).
- Грабля инструментария: вывод bash-пайплайна съедает «[m» из «[main]» как
  ANSI-reset → казалось, что branches-фильтр workflows испорчен («ain]»);
  Read-инструментом файлы оказались целыми. Урок: подозрение на порчу
  bracket-синтаксиса — проверять Read'ом, не bash-выводом.

Stage Summary:
- Деплой восстановлен, на проде коммит 8d3eb34 (весь пакет c89 владельца).
- Ретраи rsync в deploy.yml защитят от повторения transient-блокировок.

---
Task ID: c91
Agent: orchestrator
Task: 4 правки владельца (кнопка наверх/фото основателя/модалка видео/Vercel).

Work Log:
- BackToTop: кнопка целиком лежала в полосе вертикального бренд-рейла
  (fixed left-0 72px, z-60 > z-50 кнопки, кликабельный <a>) — клики
  уходили в рейл. Фикс: lg:left-[92px] (72+20px), мобайл без изменений.
- Фото основателя: возвращён исходный founder-scene-v4 (Дмитрий у стола),
  вычищенный от подтёков на стекле (бокалы/клош): AI-заплатки только по
  bbox разводов (VLM-сетка 5x4), эллиптический feather-paste; лицо/
  руки/сервировка пиксельно нетронуты (замер: дифф лица 0.00, изменено
  1.5% пикселей; VLM после/до: разводы ушли, швов нет) → v6.webp;
  компонент снова на v6 с исходным alt. Урок: VLM-bbox в свободных
  координатах глючит (числа вне кропа) — сетка с номерами ячеек даёт
  надёжные координаты; AI-редактирование кропом меняет ВСЮ зону
  (перегенерация) — вклейка только малых заплаток по маске.
- Модалка видео-карусели («перекрывает карусель когда смотришь»):
  фон rgba(0,0,0,.92) → #000 (яркая карусель просвечивала сквозь 8%),
  z-index 95 → 9990 (над всеми интерактивными слоями), touch-action
  none на бэкдропе + auto на видео-обёртке, скролл-лок body+html
  (iOS игнорирует body-only), вертикальный рейл в inert-списке.
- Vercel: деплои BLOCKED с aa62892 — Hobby блокирует git-авторов не из
  аккаунта (агент «Z User <z@container>»; коммиты владельца шли норм).
  Фикс: job deploy-vercel в deploy.yml (bunx vercel deploy --prod по
  токену VERCEL_TOKEN из secrets — автор-агностичен); env проекта
  Vercel дополнены (Metrika 112532826, Yandex/Google verification,
  HOSTING_TARGET); git-автор репо переключён на верифицированный email
  аккаунта (нативная git-интеграция тоже перестанет блокироваться).
- lint/tsc/build чистые; founder-scene-v6 + z9990/#000/left-[92px] —
  в out/.

Stage Summary:
- 4/4 правки владельца в проде (после пуша): кнопка правее рейла,
  фото основателя без разводов, видео-модалка чёрный-фон/z-9990/iOS-лок,
  деплой идёт на SpaceWeb И Vercel.

---
Task ID: c92
Agent: orchestrator
Task: юзер: «телеграм канал поставь пока @nilov_catering; макс
https://max.ru/u/f9LHodD0…; и проверь на ошибки сайт».

Work Log:
- Телеграм-канал: @nilov_official → @nilov_catering (t.me 200 OK) —
  CONTACTS/SOCIALS (sameAs JSON-LD)/подписи в контактах+футере,
  llms.txt +строка. Комментарии layout/hacc-booking/site-footer синхронны.
- Макс: max.ru-заглушка → реальный профиль max.ru/u/f9LHodD0… (200 OK
  под браузерным UA; дефолтный curl ловит 403 bot-щита max.ru — SvelteKit
  SPA). Номер +7 911 826-39-26 остаётся подписью ссылки (контакты+футер).
- analytics: max.ru добавлен в MESSENGER_HOSTS_RE (цель MESSENGER_CLICK);
  ранее исключён осознанно (профиля не было — F2/cycle-71).
- ПРОВЕРКА НА ОШИБКИ (главная находка): /offer, /privacy, /terms на проде
  отдавали 403 Forbidden ВСЁ ВРЕМЯ с c69! Next.js static export кладёт
  offer.html + КАТАЛОГ offer/ (только RSC-поладки __next.*.txt, БЕЗ
  index.html): Apache mod_dir DirectorySlash на /offer даёт 301 → /offer/
  (в Location подставляет http:// — Apache за nginx-фронтом видит http),
  а в каталоге нет индекса → 403. Футер/sitemap/llms ссылаются на эти URL.
  Фикс в 2 шага (53f0e2c + cc5b5e3): public/.htaccess — per-dir
  RewriteRule ^(offer|privacy|terms)/?$ $1.html [L] + DirectorySlash Off
  (на этом хостинге mod_dir redirect успевает раньше per-dir rewrite) +
  ErrorDocument 404 /404.html (была дефолтная «Object not found!»).
  Итог: /offer /privacy /terms → 200 ПРЯМО по https, без редиректов.
  СОЗНАТЕЛЬНО без http→https редиректа: %{HTTPS} у Apache всегда off
  (SSL терминирует nginx) — правило даст бесконечный цикл; канонизация
  обеспечена rel=canonical. Экспорт копирует .htaccess-dotfile в out/.
- Vercel-зеркало: canonical/og:url указывали на самого себя
  (newsite-three-kappa.vercel.app) — NEXT_PUBLIC_SITE_URL в env проекта
  существовал, но ПУСТОЙ (пустая строка → ||-фолбэк). Upsert через
  Vercel API (v10 env, production+preview) = https://nilovcatering.ru;
  после ребилда зеркало каноникалится на основной домен.
- Верификация (agent-browser на живом проде + curl): lint/tsc/build
  чистые; CI+deploy+deploy-vercel success ×3 пуши; в гидратированном DOM
  t.me/nilov_catering ×3 (контакты/футер/инста-ряд) + JSON-LD,
  max.ru/u/… ×2 (контакты+футер), stale 0; клик по футеру «Публичная
  оферта» → /offer 200 https; калькулятор 50×4470=223 500 ₽ верно;
  видео-модала z-9990/#000/скролл-лок, видео играет по настоящему клику
  (пауза при synthetic-eval-click — артефакт headless, не баг);
  BackToTop x=92; ImageTrail жив (3 пустых .it-img = idle-пул эффекта,
  src появляются при движении мыши — by design); мобайл 390px без
  горизонтального скролла; консоль/ошибки страницы пустые; VLM-скрины
  десктоп+мобайл: NO DEFECTS; медиа-сэмплы 200.

Stage Summary:
- TG-канал @nilov_catering и MAX-профиль владельца — в проде (SpaceWeb
  + Vercel-зеркало), аналитика кликов Макса включена.
- КРИТИЧЕСКИЙ застарелый баг закрыт: юрстраницы (оферта/политика/условия)
  403 со времён c69 → теперь 200 напрямую по https (+ брендированная 404).
- Vercel-зеркало корректно каноникалится на nilovcatering.ru.
- Коммиты: 640c202 (TG+Max), 53f0e2c (.htaccess), cc5b5e3 (DirectorySlash
  Off); все задеплоены и проверены на живом сайте.

---
Task ID: c93
Agent: orchestrator
Task: юзер: «добавь логотип компании в каждую пдф; проверь и исправь
ошибки на сайте; правки от заказчика: фуллскрин видео — теряется
курсор (надо Escape); маленькая фотка — размытие пикселей; заголовок
„Соберите банкет“ → „Соберите меню“».

Work Log:
- PDF-логотип (pdf-client.ts): loadLogos() — модульный кэш fetch
  эмблем (emblem-white-480 / emblem-black-480, data-URL); сбой сети
  НЕ валит генерацию (декор — тихая деградация). Вёрстка: бренд-полоса
  1-й страницы — БЕЛАЯ эмблема 26мм справа (поля 5мм, «food as art»
  уезжает левее с 5мм воздуха); страницы продолжения (≥2) — ЧЁРНАЯ
  эмблема 8мм в правом верхнем углу (пост-проход drawPageFooters).
  jsPDF 4.x сам дедуплицирует повторные addImage (замер: 10 страниц =
  56KB — PNG вклеен один раз). Тесты: bun-скрипт с мокнутым fetch →
  каталог 9 стр. 275KB + пакет 2 стр. 416KB; pdftoppm-рендер + VLM:
  эмблемы на месте, не клипаются, не конфликтуют; в браузере кнопка
  «Каталог в PDF» — fetch шрифтов ×5 + эмблем ×2 = 200, консоль чистая.
- Фуллскрин-видео (заказчик: «теряется курсор, надо нажимать escape»):
  КОРЕНЬ — globals.css прячет системный курсор глобально
  (body.catering-cursor * {cursor:none!important}), а кастомная линза
  (z-9999, обычный DOM) НЕ рисуется в top layer браузера при нативном
  фуллскрине <video> → в фуллскрине курсора НЕТ вообще. Фикс:
  :fullscreen/-webkit-full-screen + потомки → cursor:auto!important
  (специфичность (0,2,1) > (0,1,1) у «*»-правила). Плюс ESC-логика
  модалки: document.fullscreenElement||webkitFullscreenElement активен →
  первый Esc только выходит из фуллскрина (браузер), модалку не закрывает
  (раньше Esc закрывал оба слоя разом). Верификация: правило в бандле
  (Lightning CSS держит :is(...)); requestFullscreen в headless запрещён
  (Permissions check failed) — поведение проверено логикой специфичности
  + регрессия обычного Esc (модалка закрывается) PASS.
- Фото основателя v7 (заказчик: «на маленькой фотке размытие
  пикселей»): карта резкости (std лапласиана по тайлам 80px) показала
  падение в зонах AI-заплаток c91 — бокалы 87 (v4: 94), клош 73 (v4:
  87); VLM подтвердил «mushy/smoothed» на стекле. v7 = v4 +
  детерминированная чистка: вертикальные молочные подтёки вычтены
  через горизонтальную медиану (k=15, сдвиговый стек; яркостная маска
  6<lum<60, только bbox разводов, интенсивность 85%, антиалиасинг
  3×3, фезеринг границ боксов 8px). Зерно/резкость нетронуты: бокалы
  85 / клош 80 / лицо 71.4 (v4: 94/87/71.7 — до v6: 87/73/71).
  Итерация с дилатацией маски забракована (резкость бокалов падала
  до 65). VLM: разводы ушли, артефактов/пластика нет, на 270px-слоте
  резко и профессионально. Компонент → founder-scene-v7.webp.
- Заголовок калькулятора: «Соберите банкет.» → «Соберите меню.» в
  ОБОИХ местах (живой виджет hacc-booking.tsx + SSR-шелл page.tsx —
  1:1 зеркало, иначе свап фоллбэка двигал бы заголовок); комментарии
  tsx/css синхронизированы.
- Проверка на ошибки: lint/tsc/build чистые; agent-browser на локальном
  статик-экспорте (out/, порт 3999): страница errors/console пустые,
  84/84 внутренних URL = 200 (все img/video/poster/якоря), фулл-скрин
  десктоп + 390px мобайл — VLM NO DEFECTS, горизонтального скролла
  нет; внешние ссылки живые (t.me/nilov_catering, max.ru профиль,
  /offer /privacy /terms = 200 на проде).
- Тестовые скрипты (scripts/test-pdf-*.ts) удалены после верификации.

Stage Summary:
- Логотип компании теперь в КАЖДОМ PDF (каталог и per-tariff): большая
  белая эмблема в бренд-полосе титула + малая чёрная на страницах
  продолжения; деградация без сети — молча, без поломки кнопки.
- Фуллскрин видео: системный курсор возвращён (CSS), Esc двухступенчатый
  (фуллскрин → модалка), регрессий нет.
- Маленькая фотка основателя: v7 — разводы чистые, резкость исходника
  (провал v6 в клоше 73→80, бокалы 87→85 при v4=94/87).
- Заголовок раздела калькулятора: «Соберите меню. Чек напечатается
  сразу.» (виджет + SSR-шелл синхронны).

---
Task ID: c94
Agent: orchestrator (2 волны слепых критиков: A-код/данные, B-живой UI, C-финальный гейт)
Task: юзер: «под картой — просим позвонить заранее; реворк допуслуг
(оборудование +15%, мебель +20% к смете, услуги — „от“); в PDF поменять
домен interfood → nilovcatering.ru; проверить сайт на такие ошибки
критиками».

Work Log:
- Допуслуги (pricing.ts): Addon {price?, percent?}; оборудование +15%,
  мебель +20% от ПОДЫТОГА меню (гости × цена/чел — масштабируется с
  событием: 200 гостей банкет = мебель 178 800 ₽ вместо прежних фикс
  15 000 ₽ — недочёт заказчика «фуршет на 200 человек, а мебели на
  15 000»); шеф 28к/шоу 35к/бар 32к/флористика 25к — «от N ₽».
  Хелперы addonAmount/addonNote — единая точка правды (calcTotal,
  карточки, чек, лид, сводка; calculator.tsx легаси тоже переведён).
- UI калькулятора (hacc-booking.tsx): карточки «+20% ≈ 88 000 ₽» (живой
  пересчёт) / «от N ₽»; строки чека «Аренда мебели · 20% → +N ₽», фиксы
  «от N ₽»; итог «Итого от» при фикс-допуслугах (hasFromAddons) —
  волна-2 довела «от» до бара/SR-live/successMeta/лида («Расчёт с
  сайта: от N ₽»); лид/сводка — «Аренда мебели (+20% ≈ 88 000 ₽)»;
  sig процентных строк включает сумму (перепечать при смене гостей).
- Подпись под картой (site-footer.tsx): «Просим вас позвонить нам
  заранее, чтобы договориться о встрече» — tel:+79119417205 на «позвонить
  нам заранее» (золото футера, тон «открыть в Яндекс.Картах»).
- PDF (pdf-client.ts): SITE_DOMAIN interfood-catering.ru →
  nilovcatering.ru. Верификация: bun-генерация + pdftotext — 10×
  nilovcatering.ru (каталог 9 стр.) + 2× (пакет), 0× interfood.
- Фиксы волны критиков: A-MAJOR — сводка шага 2 в «Ещё решаю» печатала
  «(+20% ≈ 0 ₽)» (гейт !undecided, как у лида; выбор допуслуг живёт
  в стейте и честно возвращается при выборе формата); B-D1 MAJOR —
  cookie-баннер накрывал CTA «Оставить заявку» sticky-панели на 720p
  ВСЁ время прилипания (замер: ctaTop залип 625 при баннере 501–696,
  ~840px скролла) — при body.cookie-banner-open на ≥1024px панель
  отлипает (relative + top:auto, контекст штампа сохранён; приняли
  cookies → sticky возвращается, замер подтверждён); A-INFO — фоллбек
  NEXT_PUBLIC_SITE_URL → nilovcatering.ru (config.ts + site-config.ts,
  был vercel-зеркало); C-NIT — событие calc-lead в «Ещё решаю»
  зеркалит письмо (addonIds: []).
- Волна-2 (критик C): APPROVE — спека 7/7, арифметика копейка-в-копейку
  в 7 сценариях (329 725 / 301 725 / 50 000-кламп / 120 690 / 148 690 /
  1 234 900), tsc 0, консоль чистая, VLM NO DEFECTS.
- Отложено (зафиксировано): тик «500» слайдера выступает за рельс на
  15px на мобиле — принятый c83-F2 дизайн (лейбл центрован на позиции
  шкалы, вьюпорт не переполняется); хардкод-цены панелей услуг
  (Свадьбы 5 500/Корпоратив 2 500 без calcType) — c89-осознанное;
  21 мёртвый компонент со старым брендом — не рендерятся.

Stage Summary:
- Допуслуги масштабируются со сметой (заказчик), «от» на услугах,
  домен в PDF = nilovcatering.ru, подпись под картой, 2 MAJOR-фикса
  (undecided-сводка, cookie×sticky-CTA на 720p). lint/tsc чистые,
  3 критика (A/B/C) — финальный APPROVE.

---
Task ID: c95
Agent: orchestrator (реализаторы 1-a/1-b/1-c/1-d/1-e + 2 волны слепых критиков + фикс-волны)
Task: юзер: «админ панель где заказчик может менять все меню и цены с
синхронизацией сайт+калькулятор+PDF; хостинг spaceweb — выбрать БД; проверить
все формы/кнопки; заявки желательно владельцу в Telegram».

Work Log (сжато):
- Разведка: заявки были mailto (реальной отправки нет), в contact.tsx
  хардкод старой почты (исправлен). Архитектура: БД = git (src/data/menu.json)
  + серверные JSON (server-data/, вне rsync --delete); PHP-API same-origin;
  публикация = коммит через GitHub Contents API → CI → rsync.
- Реализовано волнами субагентов: (1-a) слой данных menu.json + zod +
  гейты build (llms/meta/плитки синхронизируются); (1-b) админ-панель /admin
  (логин HMAC, редактор, заявки, настройки, мастер TG, статус публикации);
  (1-c) PHP-API lead.php/admin.php (смоук 67/67→68/68); (1-d) submitLead +
  мейлто-фолбэк; (1-e) deploy.yml (rsync-исключения, Provision secrets по SSH,
  Verify), vercel.json redirects (rewrites после ФС — нельзя), robots.
- Секреты: GH Actions Secrets через API (libsodium); CI пишет
  config/secrets.php на хостинг (вне git).
- e2e: браузер на реальном статик-экспорте (php -S) — правка → публикация →
  деплой success; лид-форма → 200 → хранение; прод golden-path: заявка
  «ЗАЯВКА ПРИНЯТА № 1709-0447». Пойман и исправлен Origin 127.0.0.1 (403).
- Волна критиков 1 (A/C/D): XFF-спуфинг rate-limit (CRITICAL, фикс: последний
  хоп XFF + глобальные предохранители), неотзываемые сессии (эпоха),
  мёртвое поле заголовка плиток, ложный красный CI (llms-гейт снят),
  статус не по workflow (deploy.yml-фильтр + состояние cancelled),
  4-пробельные диффы (2-пробельный pretty). Всё исправлено, смоук зелёный.
- Волна критиков 2 (E/F/G): E — APPROVE (требование: владелец сузит PAT —
  README; миноры исправлены: logout-эпоха, login-предохранитель, капы цен);
  F — полный обход владельца (63 скриншота): транслитерация поиска галереи;
  G — интеграция подтверждена D + оркестратором (пересборка в worktree:
  цены/мета/llms/плитки/PDF обновляются, калькулятор сходится копейка-в-копейку).

Stage Summary:
- Админка: nilovcatering.ru/admin (пароль у владельца; смена в «Настройках»).
- Публикация ~3-7 мин, статус с поллингом, черновики в localStorage.
- Заявки: server-data/leads.json + email + Telegram (мастер в «Настройках»,
  токен BotFather). TG-API из РФ нестабилен с 03.2026 — есть поле api-base.
- Открыто (владельцу): пересоздать GitHub PAT с contents-only (без workflow
  scope) и обновить секрет ADMIN_GH_TOKEN; возможно настроить TG-бота.
- Инструкция владельцу: README-ADMIN.md. API-контракт: public/api/README.md.

---
Task ID: c96
Agent: orchestrator (+ research subagent c96-TG-RESEARCH)
Task: юзер: «в админке нельзя скролить вниз — исправь; максимально расширь
админку для заказчика; протестируй всё; TG-токен дал ошибку "Не удалось
связаться с Telegram"; протестируй все формы сайта».

Work Log:
- ДИАГНОЗ СКРОЛЛА: stopSiteLenis() звал __lenis.stop() — а stop() в lenis
  1.x делает preventDefault на ВСЕ wheel/touch (onVirtualScroll:
  isStopped → e.preventDefault()) + класс lenis-stopped (globals.css:
  .lenis.lenis-stopped { overflow: hidden }) → админка нескроллируема
  ВООБЩЕ. В headless-тестах воспроизводства не было (prefers-reduced-
  motion → Lenis не создавался) — поэтому пропустили волны критиков c95.
- ФИКС: LenisProvider — isAdmin-гард по usePathname (Lenis на /admin не
  создаётся; deps [lite, isAdmin] → SPA-навигация пересобирает эффект,
  destroy() при уходе на /admin снимает listeners и lenis-классы);
  admin/page.tsx — stop-полл удалён, defense-destroy + CSS-страховка
  html[data-admin-page].lenis-stopped { overflow: auto !important }.
- TELEGRAM (исследование c96-TG-RESEARCH, research/c96/tg-mirrors.md):
  публичных зеркал Bot API НЕТ (7 кандидатов проверены curl; telega.one —
  ловушка, не Bot API; остальные мертвы; структурная причина: токен в URL
  → публичный прокси = перехватчик токенов). Решение: СВОЙ Cloudflare
  Worker (код research/c96/tg-api-worker.js, проверен 7/7 локально).
  PHP: tg_api_bases() (запомненная tgWorkingBase → tgApiBase владельца →
  secrets tg_api_base → api.telegram.org → tg_api_bases[]) +
  tg_api_try() (переключение ТОЛЬКО по транспортным сбоям/5xx; 401/400/
  429 = TG жив) + tg_remember_working_base() (flock, идемпотентна) +
  tg_send_fb() (lead.php и админ-мастер ходят через цепочку).
  tg-check/discover/test возвращают 'tried' (errno+ошибка каждой базы)
  и 'base' — UI показывает диагностику.
- UI настроек TG: сетевая ошибка = объясняющий блок («Хостинг не смог
  связаться… блокировка api.telegram.org для РФ-серверов, интернет и
  токен ни при чём») + поле «адрес зеркала» с кнопкой «Сохранить и
  проверить» (сразу re-check токена) + раскрывающаяся пошаговая
  инструкция CF Worker с кодом и «Скопировать код» + «Сейчас уведомления
  уходят через: <база>» в «Дополнительно».
- РАСШИРЕНИЕ АДМИНКИ: редактор меню — поиск по форматам/пакетам/блюдам
  (найденное раскрывается), «Развернуть/Свернуть всё», массовые цены
  (± % на всё/формат, округление 10/50/100, превью 2 450→2 700, капы
  zod 100..100000), дубли блюда ⧉ и «Дублировать пакет», счётчики «3
  пакета · 47 блюд» и «~1 240 г на гостя». Заявки — фильтр «Все/Новые»,
  статистика (всего/сегодня/непрочитанных), копирование телефона ⧉,
  «Прочитать все» (новый action leads-read-all), экспорт CSV (BOM, «;»).
  Глобально — beforeunload-гард при dirty, диалог публикации со сводкой
  «что изменится» (computeMenuDiff: цены/составы/добавления, до 30 строк).
- ТЕСТЫ: php-smoke 68/68 PASS; research/c96/c96-tests.sh 16/16 PASS
  (фолбэк мёртвое зеркало→main=401 unauthorized + base в ответе; порядок
  и дедуп tg_api_bases; remember идемпотентность+валидация; read-all
  3 лида→updated=3→0; без auth 401); tsc 0, lint 0; браузерный e2e на
  статик-экспорте (php -S :3005, реальный GitHub-раундтрип на
  research/c95/test-menu.json): логин, СКРОЛЛ 0→800→2000 из 2928 +
  колесо мыши, поиск «банкет»→1 из 6 карточек, массовое −5% Банкет
  (4470→4250 в заголовке), диалог публикации с 4 ценами в диффе, форма
  заявки сайта «Заявка принята № 1709-0919» → лид в leads.json с payload
  (banquet/30 гостей/134100), раздел заявок (фильтры/копирование/read-
  all/CSV), настройки (3 шага TG), TG-сетевая ошибка на блок-сервере
  (api.telegram.org→10.255.255.1): блок ошибки + инструкция + «Попытки:
  Connection timed out after 5002 ms», мобайл 390px без горизонтального
  скролла, консоль/errors чистые. Сайт (не админка) скроллится как раньше.

Stage Summary:
- Скролл админки починен (Lenis на /admin не инициализируется + страховка).
- TG: публичных зеркал нет; владелец разворачивает свой CF Worker по
  встроенной инструкции (5–10 мин); сервер сам перебирает базы и
  запоминает рабочую; ошибки диагностируются в UI.
- Админка расширена (поиск/масс-цены/дубли/фильтры/CSV/read-all/сводка
  публикации/beforeunload). Все формы и кнопки протестированы e2e.
- Далее: коммит+push, проверка прода.

---
Task ID: c96-WAVE2
Agent: orchestrator (критик A сабагент + фикс-волна + прод-верификация)
Task: волна 1 критиков c96 и фиксы; проверка прода после деплоя.

Work Log:
- Критик A (c96-CRIT-A, слепой, research/c96/critA/REPORT.md): NEEDS FIX —
  3 MAJOR: (1) 3xx-редирект-база блокирует весь TG-фолбэк (t.me) и
  tgWorkingBase запоминается на 401/404-«ловушках» без сброса;
  (2) CSV-инъекция формул Excel («=HYPERLINK…») + экранирование ''
  вместо RFC-4180; (3) бюджет таймаутов lead.php без капа — 2 мёртвые
  базы = 10.6 с ожидания посетителя. MINOR: read-all затирает битый
  leads.json пустышкой; превью массовых цен ≠ применение (кламп только
  в apply); дифф пакетов по имени; «Сохранить и проверить» без токена —
  ложный «сетевой сбой».
- ФИКСЫ (a80c397): tg_api_try — только 2xx = «база отвечает» (3xx/4xx/5xx
  → следующая); remember строго на 200-ok (getMe/getUpdates/sendMessage);
  tg_forget_working_base() при полном сетевом сбое (самозалечивание);
  tg_send_fb maxBases (lead.php = 2); lead.php — заявка записана →
  ответ посетителю СРАЗУ (fastcgi_finish_request на FPM-хостинге),
  уведомления догоняют в фоне; CSV — RFC-4180 «"» + префикс «'» для
  ячеек с =/+/-/@; read-all не трогает битый leads.json (500 storage);
  превью массовых цен = применение (кламп 100..100000 в обоих);
  no_token → «Сначала вставьте токен».
- Верификация фиксов: research/c96/critA-fix-tests.sh 5/5 PASS (t.me
  пропускается и фолбэк доходит до api.telegram.org; forget работает;
  maxBases=2 из 3; 404-ловушка не запоминается); смоук 68/68 повторно;
  c96-тесты 16/16; tsc/lint/build чистые; браузерный ре-тест формы
  после реструктуризации lead.php — «Заявка принята № 1709-1018».
- Критик B (c96-CRIT-B, UX, 20 скриншотов) — умер по лимиту ходов ДО
  написания отчёта; проверил по скриншотам (VLM): 3 OK, 1 транзиентный
  тост поверх шапки (норма sonner, исчезает сам). Его сценарии
  (масс-цены с отрицательным %, мобайл с dirty-баром, шит «Ещё»,
  cookie-баннер, форма на мобиле) — без блокеров.
- ПРОД (a80c397, build-and-deploy success: rsync+verify зелёные):
  / и /admin = 200; чанк с «Поиск по меню» задеплоен; /data/menu.json
  md5 == src (5a13d2ff); lead.php GET 405, /server-data /config 403;
  ЖИВАЯ заявка на проде через реальную форму — «Заявка принята
  № 1709-1131» (имя «ТЕСТ ФОРМЫ (можно удалить)»), без console errors.
  Vercel-зеркало (вторичный job) подвис в in_progress — не влияет на
  основной домен.

Stage Summary:
- Оба коммита c96 (fdaa067 + a80c397) в проде, всё зелёное.
- Заявки: посетитель не ждёт TG (фон), владелец получает TG через
  своё зеркало по встроенной инструкции + email-фолбэк всегда.
- Открытое (владельцу): развернуть CF Worker по инструкции в админке
  (шаг 1 мастера TG) — займёт 5–10 минут; тест-заявку № 1709-1131
  можно удалить в разделе «Заявки».

---
Task ID: c97
Agent: orchestrator
Task: юзер: «телеграм работает, но почта не работает — ни клиенту ничего
не приходит, ни мне заявки на почту; проверь всю работоспособность,
протестируй, чтобы неожиданно не находить нерабочее».

Work Log:
- ДИАГНОЗ (по коду c95/c96): (1) клиенту подтверждение НЕ ОТПРАВЛЯЛОСЬ
  ВООБЩЕ — функции не существовало; (2) письмо владельцу шло через mail()
  БЕЗ Date/Message-ID и с одноблочной encoded-темой — главные спам-сигналы
  Gmail (notifyEmail=…gmail.com) — вероятная причина «ничего не приходит»;
  (3) статус доставки нигде не сохранялся и не показывался.
- _lib.php: smtp_send() — SMTP-клиент на raw sockets (без phpmailer):
  SSL:465 (implicit) и STARTTLS (587), AUTH LOGIN, многострочные ответы,
  dot-stuffing (RFC 5321), MAIL FROM=From (DKIM-выравнивание);
  mail_subject_enc() — encoded-word чанками ≤75 байт (RFC 2047, PCRE —
  без mb_str_split-зависимости); mail_headers() — Date/Message-ID
  обязательны; mail_body_crlf() — нормализация CRLF + защита точек;
  mail_send() — ЕДИНАЯ точка: SMTP (если настроен) → фолбэк mail()
  («mail-fallback», письмо не теряем при сбое SMTP) → честный отказ;
  журнал mail-log.json (cap 300, новые сверху); настройки smtpHost/Port/
  User/Pass/From (пароль — маска в UI, smtpPort нормализуется 1..65535);
  smtp_config() — дефолт порта 465, From=юзер (битый smtpFrom игнорируется).
- lead.php: lead_notify_all() → массив статусов {tg,mail,client,
  mailTransport}; НОВОЕ — подтверждение КЛИЕНТУ на его email (копия
  расчёта, контакты, Reply-To=владелец — ответ клиента приходит
  владельцу напрямую); lead_notify_and_store_status() — статусы
  дописываются в запись лида (leads.json, notify-поле) под тем же локом.
- admin.php: action=mail-test (тест тем же каналом, rate 10/час,
  возвращает transport/ошибку/детали); action=mail-log (последние
  отправки, лимит 1..50); settings GET/POST — SMTP-поля с валидацией
  (host hostname-регэксп, порт 1..65535, юзер email, пустой пароль
  НЕ затирает — clearSmtpPass для очистки); mask_secret().
- Админ UI: EmailCard — «Проверка почты» (тест-кнопка + внятные
  объяснения ошибок, подсказка про «Спам»), «Последние отправки»
  (журнал, контексты «уведомление вам/подтверждение клиенту/тест»),
  SMTP-блок с инструкцией SpaceWeb (панель → Почта → Ящики → noreply@…,
  smtp.spaceweb.ru:465) и статусом «включён»; в «Заявках» — бейджи
  доставки каждой заявки (Telegram / почта (SMTP) / клиенту).
- Бонус-фиксы: computeMenuDiff — падение на пакетах без dishes
  (?.length ?? 0) и пересчёт диффа только при открытом диалоге
  публикации (был на каждый рендер); mail() путь больше не дублирует
  To/Subject в заголовках (mail() ставит свои).
- Тесты: research/c97/ — c97-tests.sh 40/40 PASS (юниты subject/body/
  config/log; интеграция smtp_send с python fake-smtp: EHLO/AUTH/MAIL/
  RCPT/DATA/dot-stuffing/заголовки/round-trip темы; HTTP: валидация
  SMTP-полей, mail-test/mail-log, лид с email клиента → 2 письма +
  notify-статусы в leads.json); c97-e2e.sh (agent-browser) 21/21 PASS —
  админка: логин → настройки → SMTP через UI → тест-письмо → журнал;
  сайт: форма с email клиента → «заявка принята» → 2 письма (владельцу
  + клиенту с приветствием по имени) → бейджи в «Заявках»; консоль и
  page errors чистые; VLM-скрины — NO DEFECTS. Регрессии: c95 68/68,
  c96 16/16 + 5/5; tsc/lint/build чистые.
- Грабли зафиксированы: (а) песочница убивает фоновые процессы между
  вызовами bash — e2e-стенд живёт только внутри одного вызова; (б)
  админ-панель требует ГИТХАБ-файл меню (data-action) — e2e-фикстура =
  РЕАЛЬНОЕ src/data/menu.json (минимальная роняла computeMenuDiff);
  (в) innerText возвращает РЕНДЕРЕННЫЙ регистр (CSS uppercase) — grep
  в e2e только case-insensitive; кнопка «Заявки» содержит счётчик
  непрочитанных → матчится по префиксу; (г) eval-и agent-browser делят
  глобальный скоуп страницы — все оборачивать в IIFE.

Stage Summary:
- Клиент с email теперь получает подтверждение заявки; владелец — письма
  с правильными заголовками (Date/Message-ID) через SMTP или sendmail.
- Вся доставка прозрачна: бейджи в заявках + журнал + тест-кнопка в
  настройках — «неожиданно нерабочей» почта больше не бывает.
- Владельцу для гарантии инбокса: создать ящик в панели SpaceWeb и
  вписать в «Настройки → SMTP» (инструкция в UI и README-ADMIN.md).
- Всё задеплоено на прод (см. следующий шаг — деплой и прод-тесты).

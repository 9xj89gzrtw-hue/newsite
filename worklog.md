# Worklog — Catering Project (сжатый c89; полная история: git log worklog.md, docs/AGENTS-HISTORY.md)

## Индекс прошлых задач (детали: git log, тег Task ID)
2-research · 2-A/2-B · 3-A/3-B/3-C · 4-C/4-D/4-E · 5 · 6-A…6-F · 7-A/7-B · cycle-26…48 · 58…63 · c64-c87 (AGENTS.md §36-§56) · c88 · c89

Ключевые вехи: 6-x — ресёрч с 22 эталонов; cycle-26+ — витрина; c55 — фото-конвейер;
c69 — ребрендинг субагентами; c71 — LLM-SEO; c80 — QA fresh-clone; c81 — Lighthouse+критики;
c83 — анимации везде; c84 — мобильный UX; c85 — перф/iPhone; c86 — правки владельца+критики;
c87 — 44 реальных фото владельца; c88 — фото в ленту, видео-карусель возвращена;
**c89 — большой пакет правок владельца: 6 форматов услуг, новые цены/минимальные
заказы, видео с Диска, контакты 2 колонки, реквизиты ИП Ниловой, 4 волны критиков**.

---
Task ID: c88 (сжато)
Agent: orchestrator (критики волн 1-2)
Task: видео-карусель возвращена на рендер; 44 фото — в ленту GammaMarquee
за «Кейтеринг как искусство».

Stage Summary:
- Лента: тайлы единой высоты, пропорция кадра инлайн; цикл 170с ≈ 110-115 px/s.
- Фиксы волн: z модалки 95 (кука/FAB больше не поверх), шов −50% точен.
- Гейты: 2 волны → APPROVE; lint/tsc/консоль чистые. Коммиты c88.

---
Task ID: c89
Agent: orchestrator (7 субагентов-реализаторов + 4 волны слепых критиков)
Task: большой пакет правок владельца (ауг 2026): карусель/услуги/меню/цены/
видео/калькулятор/контакты/подвал/UX. Коммит aa62892.

Work Log (сжато; полная спека — сообщение коммита aa62892):
- Карусель: −8 фото (07/10/11/22/25/28/41/44), 36 осталось, 170с→137с.
- Глава 01: один аккордеон, 6 форматов (Свадьбы/Корпоратив/Шоу-станции/
  Выездной бар/Вегетарианское и халяль/Гастро-боксы), CTA→#calculator.
- Глава 02: фикс прыжка банкетных табов (scrollTop-сохранение + анимация
  высоты рэка по паттерну is-animating/expandList); BBQ 2900/4450
  +листовой салат 150г+ассорти летних овощей 150г; веган 3200/4250; «от»
  везде (панель/табы/PDF); hookNote доставки: мин 17 400 ₽/48ч; сезонный
  коэффициент ×1,15 удалён отовсюду (pricing no-op, чек, PDF, llms).
- Видео (Яндекс.Диск, все 1080×1920): GG-showcase = 16:9 центр-кроп
  1280×720 + портрет 720×1280 для тача (паттерн tott-hero, гард по src);
  карусель = 6 вертикальных клипов c89-event-1..6 (720×1280, ≤8.4МБ),
  модалка 9:16 (height min(86vh,140vw)), постеры, VLM-честные подписи.
- Калькулятор: snack-box flat calcPerGuest=1200 (пакеты НЕ выбираются);
  MIN_ORDER (85к банкетные/50к кофе/17.4к доставка) — кламп итога +
  честная строка «Минимальный заказ формата +N ₽»; ADDONS waiters→
  furniture «Аренда мебели» 15000; сезонная строка/предупреждения удалены.
- Контакты: 2 колонки (Связь: Телефон/ТГ/Ватсап/Макс +7 911 826-39-26/
  Смс/Адрес | Соцсети: IG/VK/ТГ-канал @nilov_official/YouTube/Rutube);
  карта → подвал (lazy iframe); #contact → ContactsZone (форма = #lead-form,
  открывается только CTA); мета/JSON-LD/llms/yandex.txt синхронизированы
  (ИП Нилова Анастасия Дмитриевна, ТБанк, dmitry_nilov@mail.ru, 2007).
- UX: BackToTop возвращён (мобайл+десктоп); HoverScramble из хедера убран;
  КРИТИЧЕСКИЙ ФИКС drawer-навигации: location.hash-прыжок откатывался
  Lenis на таче → closeAndNavigate всегда через __lenis.scrollTo +
  тихий pushState (hashchange-слушатели — только косметика шапки);
  капс-лейблы 12→13px (--hb-caps-label-fs); founder-scene-v5 (без разводов).
- Верификация: Playwright-скрипты золотого пути (все числа чека сходятся);
  4 волны критиков: W1 код REJECT→фиксы (mailto лида из конфига!, llms,
  660/1200 split-brain), W1 визуал+аудитор 23/23 APPROVE, W2 код APPROVE
  (yandex.txt), W2 UX — drawer-баг найден и закрыт, W3 гейт (JSON-LD
  founder→«Дмитрий Нилов»), W4 финал APPROVE. lint/tsc/консоль чистые.

Stage Summary:
- ВСЁ из письма владельца выполнено и проверено (23/23 требований PASS).
- Ссылки-заглушки (ждём от владельца): Rutube — поиск по бренду (канал
  не найден), Макс — max.ru (диплинка по номеру нет; профиль 404).
- Меню-каталог доставки закусок показывает à la carte от 660 ₽ (пакеты),
  калькулятор — единые 1200 ₽/чел: осознанное разделение (задокументировано).
- pm2 :3001 (interfood-catering-dev) стабилен; порт 3000 — родительский
  sandbox, не трогать. ffmpeg на этой машине: только background+polling
  (foreground убивается SIGKILL по завершении — см. урок c89 в AGENTS.md).

---
Task ID: c90
Agent: orchestrator
Task: фикс упавшего деплоя nilovcatering.ru (юзер: «не получилось задеплоить»).

Work Log:
- Диагноз по GitHub API: runs aa62892/8d3eb34 (16.09) — build/export OK,
  падает шаг rsync: «Connection closed by host port 22», rc=255 (SSH-уровень).
  deploy.yml/secrets между успехом 12.09 и провалом не менялись → transient
  на стороне хостинга (блок IP раннера GitHub у SpaceWeb).
- Ручной workflow_dispatch на main → успех; живой сайт проверен: c89-маркеры
  на месте (826-39-26, 17 400, ИП Нилова), старые удалены («Как мы работаем»,
  «Сезонный коэффициент»); 10/10 sampled media = 200.
- deploy.yml: rsync-шаг получил 3 попытки с паузой 45с + ConnectTimeout=20
  + ::group::-логика (устойчивость к transient-блокам IP раннеров).
- Грабля инструментария: вывод bash-пайплайна съедает «[m» из «[main]» как
  ANSI-reset → казалось, что branches-фильтр workflows испорчен («ain]»);
  Read-инструментом файлы оказались целыми. Урок: подозрение на порчу
  bracket-синтаксиса — проверять Read'ом, не bash-выводом.

Stage Summary:
- Деплой восстановлен, на проде коммит 8d3eb34 (весь пакет c89 владельца).
- Ретраи rsync в deploy.yml защитят от повторения transient-блокировок.

---
Task ID: c91
Agent: orchestrator
Task: 4 правки владельца (кнопка наверх/фото основателя/модалка видео/Vercel).

Work Log:
- BackToTop: кнопка целиком лежала в полосе вертикального бренд-рейла
  (fixed left-0 72px, z-60 > z-50 кнопки, кликабельный <a>) — клики
  уходили в рейл. Фикс: lg:left-[92px] (72+20px), мобайл без изменений.
- Фото основателя: возвращён исходный founder-scene-v4 (Дмитрий у стола),
  вычищенный от подтёков на стекле (бокалы/клош): AI-заплатки только по
  bbox разводов (VLM-сетка 5x4), эллиптический feather-paste; лицо/
  руки/сервировка пиксельно нетронуты (замер: дифф лица 0.00, изменено
  1.5% пикселей; VLM после/до: разводы ушли, швов нет) → v6.webp;
  компонент снова на v6 с исходным alt. Урок: VLM-bbox в свободных
  координатах глючит (числа вне кропа) — сетка с номерами ячеек даёт
  надёжные координаты; AI-редактирование кропом меняет ВСЮ зону
  (перегенерация) — вклейка только малых заплаток по маске.
- Модалка видео-карусели («перекрывает карусель когда смотришь»):
  фон rgba(0,0,0,.92) → #000 (яркая карусель просвечивала сквозь 8%),
  z-index 95 → 9990 (над всеми интерактивными слоями), touch-action
  none на бэкдропе + auto на видео-обёртке, скролл-лок body+html
  (iOS игнорирует body-only), вертикальный рейл в inert-списке.
- Vercel: деплои BLOCKED с aa62892 — Hobby блокирует git-авторов не из
  аккаунта (агент «Z User <z@container>»; коммиты владельца шли норм).
  Фикс: job deploy-vercel в deploy.yml (bunx vercel deploy --prod по
  токену VERCEL_TOKEN из secrets — автор-агностичен); env проекта
  Vercel дополнены (Metrika 112532826, Yandex/Google verification,
  HOSTING_TARGET); git-автор репо переключён на верифицированный email
  аккаунта (нативная git-интеграция тоже перестанет блокироваться).
- lint/tsc/build чистые; founder-scene-v6 + z9990/#000/left-[92px] —
  в out/.

Stage Summary:
- 4/4 правки владельца в проде (после пуша): кнопка правее рейла,
  фото основателя без разводов, видео-модалка чёрный-фон/z-9990/iOS-лок,
  деплой идёт на SpaceWeb И Vercel.

---
Task ID: c92
Agent: orchestrator
Task: юзер: «телеграм канал поставь пока @nilov_catering; макс
https://max.ru/u/f9LHodD0…; и проверь на ошибки сайт».

Work Log:
- Телеграм-канал: @nilov_official → @nilov_catering (t.me 200 OK) —
  CONTACTS/SOCIALS (sameAs JSON-LD)/подписи в контактах+футере,
  llms.txt +строка. Комментарии layout/hacc-booking/site-footer синхронны.
- Макс: max.ru-заглушка → реальный профиль max.ru/u/f9LHodD0… (200 OK
  под браузерным UA; дефолтный curl ловит 403 bot-щита max.ru — SvelteKit
  SPA). Номер +7 911 826-39-26 остаётся подписью ссылки (контакты+футер).
- analytics: max.ru добавлен в MESSENGER_HOSTS_RE (цель MESSENGER_CLICK);
  ранее исключён осознанно (профиля не было — F2/cycle-71).
- ПРОВЕРКА НА ОШИБКИ (главная находка): /offer, /privacy, /terms на проде
  отдавали 403 Forbidden ВСЁ ВРЕМЯ с c69! Next.js static export кладёт
  offer.html + КАТАЛОГ offer/ (только RSC-поладки __next.*.txt, БЕЗ
  index.html): Apache mod_dir DirectorySlash на /offer даёт 301 → /offer/
  (в Location подставляет http:// — Apache за nginx-фронтом видит http),
  а в каталоге нет индекса → 403. Футер/sitemap/llms ссылаются на эти URL.
  Фикс в 2 шага (53f0e2c + cc5b5e3): public/.htaccess — per-dir
  RewriteRule ^(offer|privacy|terms)/?$ $1.html [L] + DirectorySlash Off
  (на этом хостинге mod_dir redirect успевает раньше per-dir rewrite) +
  ErrorDocument 404 /404.html (была дефолтная «Object not found!»).
  Итог: /offer /privacy /terms → 200 ПРЯМО по https, без редиректов.
  СОЗНАТЕЛЬНО без http→https редиректа: %{HTTPS} у Apache всегда off
  (SSL терминирует nginx) — правило даст бесконечный цикл; канонизация
  обеспечена rel=canonical. Экспорт копирует .htaccess-dotfile в out/.
- Vercel-зеркало: canonical/og:url указывали на самого себя
  (newsite-three-kappa.vercel.app) — NEXT_PUBLIC_SITE_URL в env проекта
  существовал, но ПУСТОЙ (пустая строка → ||-фолбэк). Upsert через
  Vercel API (v10 env, production+preview) = https://nilovcatering.ru;
  после ребилда зеркало каноникалится на основной домен.
- Верификация (agent-browser на живом проде + curl): lint/tsc/build
  чистые; CI+deploy+deploy-vercel success ×3 пуши; в гидратированном DOM
  t.me/nilov_catering ×3 (контакты/футер/инста-ряд) + JSON-LD,
  max.ru/u/… ×2 (контакты+футер), stale 0; клик по футеру «Публичная
  оферта» → /offer 200 https; калькулятор 50×4470=223 500 ₽ верно;
  видео-модала z-9990/#000/скролл-лок, видео играет по настоящему клику
  (пауза при synthetic-eval-click — артефакт headless, не баг);
  BackToTop x=92; ImageTrail жив (3 пустых .it-img = idle-пул эффекта,
  src появляются при движении мыши — by design); мобайл 390px без
  горизонтального скролла; консоль/ошибки страницы пустые; VLM-скрины
  десктоп+мобайл: NO DEFECTS; медиа-сэмплы 200.

Stage Summary:
- TG-канал @nilov_catering и MAX-профиль владельца — в проде (SpaceWeb
  + Vercel-зеркало), аналитика кликов Макса включена.
- КРИТИЧЕСКИЙ застарелый баг закрыт: юрстраницы (оферта/политика/условия)
  403 со времён c69 → теперь 200 напрямую по https (+ брендированная 404).
- Vercel-зеркало корректно каноникалится на nilovcatering.ru.
- Коммиты: 640c202 (TG+Max), 53f0e2c (.htaccess), cc5b5e3 (DirectorySlash
  Off); все задеплоены и проверены на живом сайте.

---
Task ID: c93
Agent: orchestrator
Task: юзер: «добавь логотип компании в каждую пдф; проверь и исправь
ошибки на сайте; правки от заказчика: фуллскрин видео — теряется
курсор (надо Escape); маленькая фотка — размытие пикселей; заголовок
„Соберите банкет“ → „Соберите меню“».

Work Log:
- PDF-логотип (pdf-client.ts): loadLogos() — модульный кэш fetch
  эмблем (emblem-white-480 / emblem-black-480, data-URL); сбой сети
  НЕ валит генерацию (декор — тихая деградация). Вёрстка: бренд-полоса
  1-й страницы — БЕЛАЯ эмблема 26мм справа (поля 5мм, «food as art»
  уезжает левее с 5мм воздуха); страницы продолжения (≥2) — ЧЁРНАЯ
  эмблема 8мм в правом верхнем углу (пост-проход drawPageFooters).
  jsPDF 4.x сам дедуплицирует повторные addImage (замер: 10 страниц =
  56KB — PNG вклеен один раз). Тесты: bun-скрипт с мокнутым fetch →
  каталог 9 стр. 275KB + пакет 2 стр. 416KB; pdftoppm-рендер + VLM:
  эмблемы на месте, не клипаются, не конфликтуют; в браузере кнопка
  «Каталог в PDF» — fetch шрифтов ×5 + эмблем ×2 = 200, консоль чистая.
- Фуллскрин-видео (заказчик: «теряется курсор, надо нажимать escape»):
  КОРЕНЬ — globals.css прячет системный курсор глобально
  (body.catering-cursor * {cursor:none!important}), а кастомная линза
  (z-9999, обычный DOM) НЕ рисуется в top layer браузера при нативном
  фуллскрине <video> → в фуллскрине курсора НЕТ вообще. Фикс:
  :fullscreen/-webkit-full-screen + потомки → cursor:auto!important
  (специфичность (0,2,1) > (0,1,1) у «*»-правила). Плюс ESC-логика
  модалки: document.fullscreenElement||webkitFullscreenElement активен →
  первый Esc только выходит из фуллскрина (браузер), модалку не закрывает
  (раньше Esc закрывал оба слоя разом). Верификация: правило в бандле
  (Lightning CSS держит :is(...)); requestFullscreen в headless запрещён
  (Permissions check failed) — поведение проверено логикой специфичности
  + регрессия обычного Esc (модалка закрывается) PASS.
- Фото основателя v7 (заказчик: «на маленькой фотке размытие
  пикселей»): карта резкости (std лапласиана по тайлам 80px) показала
  падение в зонах AI-заплаток c91 — бокалы 87 (v4: 94), клош 73 (v4:
  87); VLM подтвердил «mushy/smoothed» на стекле. v7 = v4 +
  детерминированная чистка: вертикальные молочные подтёки вычтены
  через горизонтальную медиану (k=15, сдвиговый стек; яркостная маска
  6<lum<60, только bbox разводов, интенсивность 85%, антиалиасинг
  3×3, фезеринг границ боксов 8px). Зерно/резкость нетронуты: бокалы
  85 / клош 80 / лицо 71.4 (v4: 94/87/71.7 — до v6: 87/73/71).
  Итерация с дилатацией маски забракована (резкость бокалов падала
  до 65). VLM: разводы ушли, артефактов/пластика нет, на 270px-слоте
  резко и профессионально. Компонент → founder-scene-v7.webp.
- Заголовок калькулятора: «Соберите банкет.» → «Соберите меню.» в
  ОБОИХ местах (живой виджет hacc-booking.tsx + SSR-шелл page.tsx —
  1:1 зеркало, иначе свап фоллбэка двигал бы заголовок); комментарии
  tsx/css синхронизированы.
- Проверка на ошибки: lint/tsc/build чистые; agent-browser на локальном
  статик-экспорте (out/, порт 3999): страница errors/console пустые,
  84/84 внутренних URL = 200 (все img/video/poster/якоря), фулл-скрин
  десктоп + 390px мобайл — VLM NO DEFECTS, горизонтального скролла
  нет; внешние ссылки живые (t.me/nilov_catering, max.ru профиль,
  /offer /privacy /terms = 200 на проде).
- Тестовые скрипты (scripts/test-pdf-*.ts) удалены после верификации.

Stage Summary:
- Логотип компании теперь в КАЖДОМ PDF (каталог и per-tariff): большая
  белая эмблема в бренд-полосе титула + малая чёрная на страницах
  продолжения; деградация без сети — молча, без поломки кнопки.
- Фуллскрин видео: системный курсор возвращён (CSS), Esc двухступенчатый
  (фуллскрин → модалка), регрессий нет.
- Маленькая фотка основателя: v7 — разводы чистые, резкость исходника
  (провал v6 в клоше 73→80, бокалы 87→85 при v4=94/87).
- Заголовок раздела калькулятора: «Соберите меню. Чек напечатается
  сразу.» (виджет + SSR-шелл синхронны).

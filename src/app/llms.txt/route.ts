/**
 * Task 1-b (cycle-71): GET /llms.txt — LLM-видимость сайта по спеке v2
 * (llmstxt.org: H1 → blockquote-саммари → абзацы фактов → H2-секции со
 * ссылками формата «- [Название](URL): описание.»).
 *
 * Факты НЕ захардкожены, а выведены из источников правды:
 *  - домен/телефон/почта/соцсети/год — src/lib/site-config.ts;
 *  - цены «от» — src/lib/pricing.ts (MENU_TYPES — те же данные, что
 *    читает калькулятор hacc-booking.tsx: llms.txt не сможет разъехаться
 *    с калькулятором).
 * c86-G: минимумы гостей сняты (заказы от одного гостя), дегустация и
 * процентные наценки не публикуются (решения владельца c86-C/C86).
 */
import { MENU_TYPES } from "@/lib/pricing";
import {
  SITE_URL_BASE,
  PHONE_E164,
  PHONE_PRETTY,
  EMAIL,
  FOUNDED_YEAR,
  EVENTS_DONE,
  GUESTS_SERVED,
  SOCIALS,
} from "@/lib/site-config";

/** «2 450» / «120 000» — русская группировка разрядов. */
function fmt(n: number): string {
  return new Intl.NumberFormat("ru-RU").format(n);
}

export function GET(): Response {
  const prices = MENU_TYPES.map(
    (m) => `${m.label.toLowerCase()} — от ${fmt(m.perGuest)} ₽/чел`,
  ).join("; ");

  const body = [
    `# nilov catering`,
    ``,
    `> Премиальный кейтеринг полного цикла в Санкт-Петербурге. С ${FOUNDED_YEAR} года — ${fmt(EVENTS_DONE)}+ мероприятий и ${fmt(GUESTS_SERVED)}+ гостей: фуршеты, банкеты, свадьбы, корпоративы, кофе-брейки, выездное барбекю и доставка закусок. «Еда как искусство» — выездной ресторан с поварами и официантами на вашей площадке.`,
    ``,
    `Ключевые факты: работаем с ${FOUNDED_YEAR} года; основатель — Дмитрий Нилов, ИП Нилов Дмитрий Игоревич; телефон ${PHONE_PRETTY} (tel:${PHONE_E164}), Telegram и WhatsApp — на этот же номер; email ${EMAIL}; офис — Санкт-Петербург, ул. Полевая-Сабировская, 45к1; заявки принимаем круглосуточно, заказы — от одного гостя. Оплата: предоплата при подтверждении заказа, окончательный расчёт — до мероприятия (условия публичной оферты); срочные заказы — решаем по звонку; вегетарианное, веганское, безглютеновое, халяль, кошер — адаптируем меню.`,
    ``,
    `Форматы и цены «от» (те же данные, что в калькуляторе на главной): ${prices}; принимаем заказы от одного гостя. В высокий сезон (май–сентябрь, декабрь) смета умножается на коэффициент ×1,15. В стоимость входит: еда, доставка в пределах КАД, сервировка, посуда и текстиль, повар и официанты на месте, лёгкое цветочное сопровождение. Не входит: аренда площадки, алкоголь, музыка, расширенная флористика — поможем организовать по запросу.`,
    ``,
    `## Основное`,
    ``,
    `- [nilov catering — главная](${SITE_URL_BASE}/): фотогалерея из 44 кадров с реальных мероприятий (число синхронизировать с EVENTS.length в ea-events-portfolio.tsx при изменении), меню и интерактивный калькулятор стоимости — смета-чек за 30 секунд.`,
    `- [Калькулятор стоимости](${SITE_URL_BASE}/#calculator): расчёт сметы по формату события, числу гостей и дате с учётом сезонного коэффициента; заявка отправляется прямо из калькулятора.`,
    `- [Меню и форматы](${SITE_URL_BASE}/#menu): пакеты «Базовый» / «Стандарт» / «Премиум» по каждому формату — состав позиций и граммовки каждой подачи.`,
    `- [Частые вопросы](${SITE_URL_BASE}/#faq): от скольких гостей принимаем заказ, сроки бронирования, что входит в стоимость, диеты и аллергии, оплата, с чего начать.`,
    ``,
    `## Документы`,
    ``,
    `- [Публичная оферта](${SITE_URL_BASE}/offer): условия оказания кейтеринговых услуг — перечень услуг, стоимость и порядок расчётов (предоплата 30%), сроки, расторжение договора, реквизиты ИП.`,
    `- [Политика конфиденциальности](${SITE_URL_BASE}/privacy): порядок обработки персональных данных в соответствии с 152-ФЗ.`,
    `- [Пользовательское соглашение](${SITE_URL_BASE}/terms): правила использования сайта и порядок заключения договора на кейтеринговые услуги.`,
    ``,
    `## Optional`,
    ``,
    `- [Instagram](${SOCIALS.instagram}): фотоотчёты с мероприятий и Reels (принадлежит Meta — признанной экстремистской и запрещённой в РФ).`,
    `- [ВКонтакте](${SOCIALS.vk}): сообщество компании, новости и кейсы.`,
    `- [Telegram](${SOCIALS.telegram}): прямая переписка с менеджером (номер ${PHONE_PRETTY}).`,
    `- [WhatsApp](${SOCIALS.whatsapp}): быстрый расчёт и бронирование (номер ${PHONE_PRETTY}).`,
    /* F2 (K3-MINOR): MAX удалён — max.ru/nilovcatering отвечает 404,
       профиля не существует (та же мёртвая ссылка была в JSON-LD sameAs). */
    ``,
  ].join("\n");

  return new Response(body, {
    headers: {
      "Content-Type": "text/markdown; charset=utf-8",
      "Cache-Control": "public, max-age=3600, s-maxage=86400",
    },
  });
}
